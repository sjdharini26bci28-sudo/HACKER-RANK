# Enter your code here. Read input from STDIN. Print output to STDOUT
import numpy as np
np.set_printoptions(legacy='1.13')
ops=['floor','ceil','rint']
myArray = np.array(list(map(float, input().split())))
for op in ops:
    print(getattr(np,op)(myArray))
