import numpy

# Step 1 & 2: Read input and convert to a list of integers
data = list(map(int, input().split()))

# Step 3 & 4: Convert to a NumPy array and reshape it to 3x3
result = numpy.array(data).reshape(3, 3)

# Print the final array
print(result)
